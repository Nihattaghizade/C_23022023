using System;
using System.Collections.Generic;
using System.Text;

namespace _23022023home
{
    internal class Features
    {
        public List<Exam> Exams = new List<Exam>();

        public List<Exam> CountofExamlessthan2()
        {
            List<Exam> exams = new List<Exam>();

            foreach (Exam p in exams)
            {
                if (  )
                {
                    exams.Add(p);
                }
            }

            return exams;
        }

        public List<Exam> ShowintegerofExamlest24hour()
        {
            List<Exam> exams = new List<Exam>();

            foreach (Exam p in exams)
            {
                if (  )
                {
                    exams.Add(p);
                }
            }

            return exams;
        }

        public List<Exam> Showinformationof1sttodayexam()
        {
            List<Exam> exams = new List<Exam>();

            foreach (Exam p in exams)
            {
                if (  )
                {
                    exams.Add(p);
                }
            }

            return exams;
        }

        public List<Exam> olubyoxsayox()
        {
            List<Exam> exams = new List<Exam>();

            foreach (  )
            {
                if (  )
                {
                    
                }
            }

            
        }

    }
}
